
  # Global Figma Setup

  This is a code bundle for Global Figma Setup. The original project is available at https://www.figma.com/design/atxcAyAXbrejI1mzr1PNnH/Global-Figma-Setup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  